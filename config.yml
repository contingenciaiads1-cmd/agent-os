version: 3.0
default_profile: iads

# Perfiles IADS — heredan del default y agregan contexto propio
profiles:
  iads:
    inherits_from: default
    description: "Ecosistema IADS — crmIads (PHP/CI4 + Baileys) + crmPrueba (React/Supabase)"

# Para agregar un perfil por repo específico:
# profiles:
#   crm-iads:
#     inherits_from: iads
#   crm-prueba:
#     inherits_from: iads
